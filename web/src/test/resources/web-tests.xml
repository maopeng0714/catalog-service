<!DOCTYPE project [
    <!ENTITY config SYSTEM "./config.xmlf">
    <!ENTITY login SYSTEM "./login.xmlf">
]>
<project basedir="." default="run-all-tests">
    <taskdef resource="webtestTaskdefs.properties" />
    <!-- for propertyregex: allows to build a message string with parameter replacement: "User {0} successfully created" -->
    <!-- usage example -->
    <!--
        <propertyregex property="updated.byAdmin"
            input="${user.updated.byAdmin}"
            regexp="\{0\}"
            replace="${it_user_signup.fullname}"
            global="false" />
        <verifytext description="verify success message" text="${updated.byAdmin}"/>
     -->
    <taskdef resource="net/sf/antcontrib/antcontrib.properties" />

    <!-- Use i18n bundle, 2nd one listed is the default -->
    <property file="../../../target/classes/ApplicationResources_${user.language}.properties"/>
    <property file="../../../target/classes/ApplicationResources.properties"/>
    <property file="../../../target/classes/displaytag_${user.language}.properties"/>
    <property file="../../../target/classes/displaytag.properties"/>

    <!-- runs all targets -->
    <target name="run-all-tests" 
        depends="Login,Logout,PasswordHint,Signup,UserTests,StaticPages,WebServices,DWR,FileUpload,HwCertTests,HwSpecTests,HwModelTests,HwMakeTests,HwArchTests,ProductTests,VendorTests,HwCertTests,HwSpecTests,HwMakeTests,HwModelTests,HwArchTests,ProductTests,VendorTests"
        description="Call and executes all test cases (targets)"/>
    
    <!-- runs user-related tests -->
    <target name="UserTests" depends="EditUser,SearchUser,SaveUser,AddUser,ActiveUsers"
        description="Call and executes all user test cases (targets)">
        <echo>Successfully ran all User UI tests!</echo>
    </target>
    
    <!-- Login to the application -->
    <target name="Login" description="Runs login test and verifies Home's Title">
        <webtest name="login">
            &config;
            <steps>
                &login;
            </steps>
        </webtest>
    </target>

    <!-- Logout of the application -->
    <target name="Logout" description="Runs logout test and verifies Login's Title">
        <webtest name="logout">
            &config;
            <steps>
                &login;
                <invoke description="get Logout Page" url="/logout"/>
                <verifytitle description="we should see the login title" text=".*${login.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Retrieve Password Hint -->
    <target name="PasswordHint" description="Gets the user's password hint">
        <webtest name="passwordHint">
            &config;
            <steps>
                <invoke description="get Login Page" url="/"/>
                <verifytitle description="we should see the login title" text=".*${login.title}.*" regex="true"/>
                <invoke description="click password hint link" url="/passwordHint?username=admin"/>
                <verifytitle description="show login title again" text=".*${login.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
    
    <target name="StaticPages" description="Ensures that StaticFilter is working">
        <webtest name="staticPages">
            &config;
            <steps>
                &login;
                <invoke description="go to static HTML page" url="/scripts/dojo/test.html"/>
                <verifytitle description="we should see static title" text="Static HTML Page" regex="true"/>
                <verifytext description="verify message" text="Static content!"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the edit user screen displays without errors -->
    <target name="EditUser"  description="Tests selecting the 'Edit Profile' forward">
        <webtest name="editUser">
            &config;
            <steps>
                &login;
                <invoke description="click Edit Profile link" url="/userform"/>
                <verifytitle description="we should see the user profile title" 
                    text=".*${userProfile.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
    
    <!-- Verify the user list screen displays without errors -->
    <target name="SearchUser" description="Tests search for and displaying all users">
        <webtest name="searchUser">
            &config;
            <steps>
                &login;
                <invoke description="click View Users link" url="/admin/users"/>
                <verifytitle description="we should see the user list title" 
                    text=".*${userList.title}.*" regex="true"/>
                <setinputfield description="set search term" name="q" value="matt_raible"/>
                <clickbutton label="${button.search}" description="Click Search"/>
                <verifyXPath
                     description="Count the number of users found"
                     xpath="count(//table[@id='users']/tbody/tr)"
                     text="1"/>
            </steps>
        </webtest>
    </target>
    
    <!-- Edit a user and then save -->
    <target name="SaveUser" description="Tests editing and saving a user">
        <webtest name="saveUser">
            &config;
            <steps>
                &login;
                <invoke description="click Edit Profile link" url="/userform"/>
                <verifytitle description="we should see the user profile title" 
                    text=".*${userProfile.title}.*" regex="true"/>
                <setinputfield description="set address" name="address.address" value="2255 Lane Avenue"/>
                <setinputfield description="set zipcode" name="address.postalCode" value="59826"/>
                <clickbutton label="${button.save}" description="Click Save"/>
                <verifytitle description="display Home if save OK"
                     text=".*${home.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
    
    <!-- Add a new user after logging in -->
    <target name="AddUser" description="Adds a new user profile">
        <webtest name="addUser">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/userform?method=Add&amp;from=list"/>
                <verifytitle description="view the user profile title" text=".*${userProfile.title}.*" regex="true"/>
                    
                <setinputfield description="set username" name="username" value="newuser"/>
<!--                 <setinputfield description="set password" name="password" value="test"/> -->
<!--                 <setinputfield description="set confirmPassword" name="confirmPassword" value="test"/> -->
                <setinputfield description="set firstName" name="firstName" value="Test"/>
                <setinputfield description="set lastName" name="lastName" value="Name"/>
                <setinputfield description="set city" name="address.city" value="Denver"/>
                <setinputfield description="select province" name="address.province" value="CO"/>
                <setselectfield description="select country" name="address.country" value="US"/>
                <setinputfield description="set postalCode" name="address.postalCode" value="80210"/>
                <setinputfield description="set email" name="email" value="newuser@raibledesigns.com"/>
                <setinputfield description="set website" name="website" value="http://raibledesigns.com"/>
                <setinputfield description="set passwordHint" name="passwordHint" value="test"/>
                <setselectfield description="select roles" name="userRoles" value="ROLE_USER"/>
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="view user list screen" text=".*${userList.title}.*" regex="true"/>
                <verifytext description="verify success message" regex="true"
                    text='&lt;div class="alert alert-success.*&gt;.*Test Name.*&lt;/div&gt;'/>
                    
                <!-- Delete user -->
                <clickLink description="Click edit user link" label="newuser"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytext description="verify success message" regex="true"
                    text='&lt;div class="alert alert-success.*&gt;.*Test Name.*&lt;/div&gt;'/>
                <verifytitle description="display user list" text=".*${userList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
    
    <!-- Sign up a new user (no login) -->
    <target name="Signup" description="Signup a new user">
        <webtest name="signup">
            &config;
            <steps>
                <invoke description="click signup link" url="/signup"/>
                <verifytitle description="view signup title" text=".*${signup.title}.*" regex="true"/>
                
                <setinputfield description="set username" name="username" value="signup-user"/>
                <setinputfield description="set password" name="password" value="test"/>
<!--                 <setinputfield description="set confirmPassword" name="confirmPassword" value="test"/> -->
                <setinputfield description="set firstName" name="firstName" value="Test"/>
                <setinputfield description="set lastName" name="lastName" value="Name"/>
                <setinputfield description="set city" name="address.city" value="Denver"/>
                <setinputfield description="set state" name="address.province" value="CO"/>
                <setselectfield description="select country" name="address.country" value="US"/>
                <setinputfield description="set postalCode" name="address.postalCode" value="80210"/>
                <setinputfield description="set email" name="email" value="signup-user@raibledesigns.com"/>
                <setinputfield description="set website" name="website" value="http://raibledesigns.com"/>
                <setinputfield description="set passwordHint" name="passwordHint" value="test"/>
                <clickbutton label="${button.register}" description="Click button 'Signup'"/>

                <verifytitle description="view main menu" text=".*${home.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${user.registered}"/>
            </steps>
        </webtest>
    </target>

    <!-- View Active Users -->
    <target name="ActiveUsers" description="View Active Users List">
        <webtest name="activeUsers">
            &config;
            <steps>
                &login;
                <invoke description="get activeUsers URL" url="/admin/activeUsers"/>
                <verifytitle description="we should see the activeUsers title" 
                    text=".*${activeUsers.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
    
    <!-- File Upload -->
    <target name="FileUpload" description="Tests uploading a file">
        <webtest name="fileUpload">
            &config;
            <steps>
                &login;
                <invoke description="click Upload a File link" url="/fileupload"/>
                <verifytitle description="we should see file upload form" text=".*${upload.title}.*" regex="true"/>
                <setinputfield description="set name" name="name" value="Canoo Test File"/>
                <setFileField description="set file" name="file" fileName="web-tests.xml"/>
                <clickbutton label="${button.upload}" description="Click Upload"/>
                <verifytitle description="show file data if upload OK" text=".*${display.title}.*" regex="true"/>
                <verifytext description="verify original filename displayed on page" text="web-tests.xml"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify CXF is configured properly. -->
    <target name="WebServices" description="Verifies CXF Services are listed">
        <webtest name="webservices">
            &config;
            <steps>
                &login;
                <invoke url="/services/"/>
                <verifytitle description="should see services listing" text="CXF - Service list.*" regex="true"/>
                <verifytext description="verify page renders" text="Available SOAP services"/>
                <verifytext description="verify UserManager exist" text="UserService"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify DWR is configured properly. -->
    <target name="DWR" description="Verifies DWR Index Page">
        <webtest name="dwr">
            &config;
            <steps>
                &login;
                <invoke url="/dwr/"/>
                <verifytitle description="should DWR Test Index" text="DWR Test Index.*" regex="true"/>
                <clicklink description="click on UserManager link" label="UserManager"/>
                <verifytext description="verify next page comes up" text="Methods For: UserManager"/>
            </steps>
        </webtest>
    </target>
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    <!--Vendor-START-->
    <!-- runs vendor-related tests -->
    <target name="VendorTests"
            depends="SearchVendors,EditVendor,SaveVendor,AddVendor,DeleteVendor"
            description="Call and executes all vendor test cases (targets)">
        <echo>Successfully ran all Vendor UI tests!</echo>
    </target>

    <!-- Verify the vendors list screen displays without errors -->
    <target name="SearchVendors" description="Tests search for and displaying all vendors">
        <webtest name="searchVendors">
            &config;
            <steps>
                &login;
                <invoke description="click View Vendor link" url="/vendors"/>
                <verifytitle description="we should see the vendorList title"
                    text=".*${vendorList.title}.*" regex="true"/>
                <setinputfield description="set search term" name="q" value="*"/>
                <clickbutton label="${button.search}" description="Click Search"/>
                <verifytitle description="we should see the vendorList title"
                    text=".*${vendorList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the edit vendor screen displays without errors -->
    <target name="EditVendor" description="Tests editing an existing Vendor's information">
        <webtest name="editVendor">
            &config;
            <steps>
                &login;
                <invoke description="View Vendor List" url="/vendors"/>
                <clicklink label="-1" description="Click edit link"/>
                <verifytitle description="we should see the vendorDetail title"
                    text=".*${vendorDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a vendor and then save -->
    <target name="SaveVendor" description="Tests editing and saving a vendor">
        <webtest name="saveVendor">
            &config;
            <steps>
                &login;
                <invoke description="click Edit Vendor link" url="/vendorform?id=-1"/>
                <verifytitle description="we should see the vendorDetail title"
                    text=".*${vendorDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <setInputField description="set name" name="name" value="HcOaKqTgUfIqBzUlDtDbTiDnVtWwRt"/>

                <clickbutton label="${button.save}" description="Click Save"/>
                <verifytitle description="Page re-appears if save successful"
                    text=".*${vendorDetail.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${vendor.updated}"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new Vendor -->
    <target name="AddVendor" description="Adds a new Vendor">
        <webtest name="addVendor">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/vendorform"/>
                <verifytitle description="we should see the vendorDetail title"
                    text=".*${vendorDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <setInputField description="set name" name="name" value="GzPtHwNhTuKxXwLcKtMjBkMzFrOeSe"/>

                <clickbutton label="${button.save}" description="Click button 'Save'"/>
                <verifytitle description="Vendor List appears if save successful"
                    text=".*${vendorList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${vendor.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing vendor -->
    <target name="DeleteVendor" description="Deletes existing Vendor">
        <webtest name="deleteVendor">
            &config;
            <steps>
                &login;
                <invoke description="click Edit Vendor link" url="/vendorform?id=-1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display Vendor List" text=".*${vendorList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${vendor.deleted}"/>
            </steps>
        </webtest>
    </target>    <!--Vendor-END-->
    <!--Product-START-->
    <!-- runs product-related tests -->
    <target name="ProductTests"
            depends="SearchProducts,EditProduct,SaveProduct,AddProduct,DeleteProduct"
            description="Call and executes all product test cases (targets)">
        <echo>Successfully ran all Product UI tests!</echo>
    </target>

    <!-- Verify the products list screen displays without errors -->
    <target name="SearchProducts" description="Tests search for and displaying all products">
        <webtest name="searchProducts">
            &config;
            <steps>
                &login;
                <invoke description="click View Product link" url="/products"/>
                <verifytitle description="we should see the productList title"
                    text=".*${productList.title}.*" regex="true"/>
                <setinputfield description="set search term" name="q" value="*"/>
                <clickbutton label="${button.search}" description="Click Search"/>
                <verifytitle description="we should see the productList title"
                    text=".*${productList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the edit product screen displays without errors -->
    <target name="EditProduct" description="Tests editing an existing Product's information">
        <webtest name="editProduct">
            &config;
            <steps>
                &login;
                <invoke description="View Product List" url="/products"/>
                <clicklink label="-1" description="Click edit link"/>
                <verifytitle description="we should see the productDetail title"
                    text=".*${productDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a product and then save -->
    <target name="SaveProduct" description="Tests editing and saving a product">
        <webtest name="saveProduct">
            &config;
            <steps>
                &login;
                <invoke description="click Edit Product link" url="/productform?id=-1"/>
                <verifytitle description="we should see the productDetail title"
                    text=".*${productDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <setInputField description="set majorVersion" name="majorVersion" value="482072251"/>
                <setInputField description="set minorVersion" name="minorVersion" value="1597611618"/>
                <setInputField description="set name" name="name" value="WlXvGfIrYgXzTzScAeMrPbAhNhDaZv"/>

                <clickbutton label="${button.save}" description="Click Save"/>
                <verifytitle description="Page re-appears if save successful"
                    text=".*${productDetail.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${product.updated}"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new Product -->
    <target name="AddProduct" description="Adds a new Product">
        <webtest name="addProduct">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/productform"/>
                <verifytitle description="we should see the productDetail title"
                    text=".*${productDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <setInputField description="set majorVersion" name="majorVersion" value="1315149229"/>
                <setInputField description="set minorVersion" name="minorVersion" value="1939548359"/>
                <setInputField description="set name" name="name" value="GqQnSgYpLeLqSvWgTnAePgNsHeLlSr"/>

                <clickbutton label="${button.save}" description="Click button 'Save'"/>
                <verifytitle description="Product List appears if save successful"
                    text=".*${productList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${product.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing product -->
    <target name="DeleteProduct" description="Deletes existing Product">
        <webtest name="deleteProduct">
            &config;
            <steps>
                &login;
                <invoke description="click Edit Product link" url="/productform?id=-1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display Product List" text=".*${productList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${product.deleted}"/>
            </steps>
        </webtest>
    </target>    <!--Product-END-->
    <!--HwArch-START-->
    <!-- runs hwArch-related tests -->
    <target name="HwArchTests"
            depends="SearchHwArches,EditHwArch,SaveHwArch,AddHwArch,DeleteHwArch"
            description="Call and executes all hwArch test cases (targets)">
        <echo>Successfully ran all HwArch UI tests!</echo>
    </target>

    <!-- Verify the hwArches list screen displays without errors -->
    <target name="SearchHwArches" description="Tests search for and displaying all hwArches">
        <webtest name="searchHwArches">
            &config;
            <steps>
                &login;
                <invoke description="click View HwArch link" url="/hwArches"/>
                <verifytitle description="we should see the hwArchList title"
                    text=".*${hwArchList.title}.*" regex="true"/>
                <setinputfield description="set search term" name="q" value="*"/>
                <clickbutton label="${button.search}" description="Click Search"/>
                <verifytitle description="we should see the hwArchList title"
                    text=".*${hwArchList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the edit hwArch screen displays without errors -->
    <target name="EditHwArch" description="Tests editing an existing HwArch's information">
        <webtest name="editHwArch">
            &config;
            <steps>
                &login;
                <invoke description="View HwArch List" url="/hwArches"/>
                <clicklink label="-1" description="Click edit link"/>
                <verifytitle description="we should see the hwArchDetail title"
                    text=".*${hwArchDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a hwArch and then save -->
    <target name="SaveHwArch" description="Tests editing and saving a hwArch">
        <webtest name="saveHwArch">
            &config;
            <steps>
                &login;
                <invoke description="click Edit HwArch link" url="/hwArchform?id=-1"/>
                <verifytitle description="we should see the hwArchDetail title"
                    text=".*${hwArchDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <setInputField description="set name" name="name" value="RfIgRzUdUeCrJmZkSeXwZfWiNfMyMc"/>

                <clickbutton label="${button.save}" description="Click Save"/>
                <verifytitle description="Page re-appears if save successful"
                    text=".*${hwArchDetail.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${hwArch.updated}"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new HwArch -->
    <target name="AddHwArch" description="Adds a new HwArch">
        <webtest name="addHwArch">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/hwArchform"/>
                <verifytitle description="we should see the hwArchDetail title"
                    text=".*${hwArchDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <setInputField description="set name" name="name" value="EfOqSzYvTbJpJgLrKsGjPaXxShGrEb"/>

                <clickbutton label="${button.save}" description="Click button 'Save'"/>
                <verifytitle description="HwArch List appears if save successful"
                    text=".*${hwArchList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${hwArch.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing hwArch -->
    <target name="DeleteHwArch" description="Deletes existing HwArch">
        <webtest name="deleteHwArch">
            &config;
            <steps>
                &login;
                <invoke description="click Edit HwArch link" url="/hwArchform?id=-1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display HwArch List" text=".*${hwArchList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${hwArch.deleted}"/>
            </steps>
        </webtest>
    </target>    <!--HwArch-END-->
    <!--HwMake-START-->
    <!-- runs hwMake-related tests -->
    <target name="HwMakeTests"
            depends="SearchHwMakes,EditHwMake,SaveHwMake,AddHwMake,DeleteHwMake"
            description="Call and executes all hwMake test cases (targets)">
        <echo>Successfully ran all HwMake UI tests!</echo>
    </target>

    <!-- Verify the hwMakes list screen displays without errors -->
    <target name="SearchHwMakes" description="Tests search for and displaying all hwMakes">
        <webtest name="searchHwMakes">
            &config;
            <steps>
                &login;
                <invoke description="click View HwMake link" url="/hwMakes"/>
                <verifytitle description="we should see the hwMakeList title"
                    text=".*${hwMakeList.title}.*" regex="true"/>
                <setinputfield description="set search term" name="q" value="*"/>
                <clickbutton label="${button.search}" description="Click Search"/>
                <verifytitle description="we should see the hwMakeList title"
                    text=".*${hwMakeList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the edit hwMake screen displays without errors -->
    <target name="EditHwMake" description="Tests editing an existing HwMake's information">
        <webtest name="editHwMake">
            &config;
            <steps>
                &login;
                <invoke description="View HwMake List" url="/hwMakes"/>
                <clicklink label="-1" description="Click edit link"/>
                <verifytitle description="we should see the hwMakeDetail title"
                    text=".*${hwMakeDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a hwMake and then save -->
    <target name="SaveHwMake" description="Tests editing and saving a hwMake">
        <webtest name="saveHwMake">
            &config;
            <steps>
                &login;
                <invoke description="click Edit HwMake link" url="/hwMakeform?id=-1"/>
                <verifytitle description="we should see the hwMakeDetail title"
                    text=".*${hwMakeDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <setInputField description="set name" name="name" value="UnIzYbWwQaQpCpJnDfQuHbBsSwOlKr"/>

                <clickbutton label="${button.save}" description="Click Save"/>
                <verifytitle description="Page re-appears if save successful"
                    text=".*${hwMakeDetail.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${hwMake.updated}"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new HwMake -->
    <target name="AddHwMake" description="Adds a new HwMake">
        <webtest name="addHwMake">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/hwMakeform"/>
                <verifytitle description="we should see the hwMakeDetail title"
                    text=".*${hwMakeDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <setInputField description="set name" name="name" value="HpAoTpIpOmCiKgGbUmRkHqLhWiGoVx"/>

                <clickbutton label="${button.save}" description="Click button 'Save'"/>
                <verifytitle description="HwMake List appears if save successful"
                    text=".*${hwMakeList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${hwMake.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing hwMake -->
    <target name="DeleteHwMake" description="Deletes existing HwMake">
        <webtest name="deleteHwMake">
            &config;
            <steps>
                &login;
                <invoke description="click Edit HwMake link" url="/hwMakeform?id=-1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display HwMake List" text=".*${hwMakeList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${hwMake.deleted}"/>
            </steps>
        </webtest>
    </target>    <!--HwMake-END-->
    <!--HwModel-START-->
    <!-- runs hwModel-related tests -->
    <target name="HwModelTests"
            depends="SearchHwModels,EditHwModel,SaveHwModel,AddHwModel,DeleteHwModel"
            description="Call and executes all hwModel test cases (targets)">
        <echo>Successfully ran all HwModel UI tests!</echo>
    </target>

    <!-- Verify the hwModels list screen displays without errors -->
    <target name="SearchHwModels" description="Tests search for and displaying all hwModels">
        <webtest name="searchHwModels">
            &config;
            <steps>
                &login;
                <invoke description="click View HwModel link" url="/hwModels"/>
                <verifytitle description="we should see the hwModelList title"
                    text=".*${hwModelList.title}.*" regex="true"/>
                <setinputfield description="set search term" name="q" value="*"/>
                <clickbutton label="${button.search}" description="Click Search"/>
                <verifytitle description="we should see the hwModelList title"
                    text=".*${hwModelList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the edit hwModel screen displays without errors -->
    <target name="EditHwModel" description="Tests editing an existing HwModel's information">
        <webtest name="editHwModel">
            &config;
            <steps>
                &login;
                <invoke description="View HwModel List" url="/hwModels"/>
                <clicklink label="-1" description="Click edit link"/>
                <verifytitle description="we should see the hwModelDetail title"
                    text=".*${hwModelDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a hwModel and then save -->
    <target name="SaveHwModel" description="Tests editing and saving a hwModel">
        <webtest name="saveHwModel">
            &config;
            <steps>
                &login;
                <invoke description="click Edit HwModel link" url="/hwModelform?id=-1"/>
                <verifytitle description="we should see the hwModelDetail title"
                    text=".*${hwModelDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <setInputField description="set name" name="name" value="IlLxDrWwJdPqVqQsXbWsFiTxFyIrEd"/>

                <clickbutton label="${button.save}" description="Click Save"/>
                <verifytitle description="Page re-appears if save successful"
                    text=".*${hwModelDetail.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${hwModel.updated}"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new HwModel -->
    <target name="AddHwModel" description="Adds a new HwModel">
        <webtest name="addHwModel">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/hwModelform"/>
                <verifytitle description="we should see the hwModelDetail title"
                    text=".*${hwModelDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <setInputField description="set name" name="name" value="OuGoAzTaPcUfUoXhVhCwBcJpFsNbEt"/>

                <clickbutton label="${button.save}" description="Click button 'Save'"/>
                <verifytitle description="HwModel List appears if save successful"
                    text=".*${hwModelList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${hwModel.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing hwModel -->
    <target name="DeleteHwModel" description="Deletes existing HwModel">
        <webtest name="deleteHwModel">
            &config;
            <steps>
                &login;
                <invoke description="click Edit HwModel link" url="/hwModelform?id=-1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display HwModel List" text=".*${hwModelList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${hwModel.deleted}"/>
            </steps>
        </webtest>
    </target>    <!--HwModel-END-->
    <!--HwSpec-START-->
    <!-- runs hwSpec-related tests -->
    <target name="HwSpecTests"
            depends="SearchHwSpecs,EditHwSpec,SaveHwSpec,AddHwSpec,DeleteHwSpec"
            description="Call and executes all hwSpec test cases (targets)">
        <echo>Successfully ran all HwSpec UI tests!</echo>
    </target>

    <!-- Verify the hwSpecs list screen displays without errors -->
    <target name="SearchHwSpecs" description="Tests search for and displaying all hwSpecs">
        <webtest name="searchHwSpecs">
            &config;
            <steps>
                &login;
                <invoke description="click View HwSpec link" url="/hwSpecs"/>
                <verifytitle description="we should see the hwSpecList title"
                    text=".*${hwSpecList.title}.*" regex="true"/>
                <setinputfield description="set search term" name="q" value="*"/>
                <clickbutton label="${button.search}" description="Click Search"/>
                <verifytitle description="we should see the hwSpecList title"
                    text=".*${hwSpecList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the edit hwSpec screen displays without errors -->
    <target name="EditHwSpec" description="Tests editing an existing HwSpec's information">
        <webtest name="editHwSpec">
            &config;
            <steps>
                &login;
                <invoke description="View HwSpec List" url="/hwSpecs"/>
                <clicklink label="-1" description="Click edit link"/>
                <verifytitle description="we should see the hwSpecDetail title"
                    text=".*${hwSpecDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a hwSpec and then save -->
    <target name="SaveHwSpec" description="Tests editing and saving a hwSpec">
        <webtest name="saveHwSpec">
            &config;
            <steps>
                &login;
                <invoke description="click Edit HwSpec link" url="/hwSpecform?id=-1"/>
                <verifytitle description="we should see the hwSpecDetail title"
                    text=".*${hwSpecDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <setInputField description="set name" name="name" value="DfPhLoTiXpDeLiXyJwAiDiYxFmUjZg"/>

                <clickbutton label="${button.save}" description="Click Save"/>
                <verifytitle description="Page re-appears if save successful"
                    text=".*${hwSpecDetail.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${hwSpec.updated}"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new HwSpec -->
    <target name="AddHwSpec" description="Adds a new HwSpec">
        <webtest name="addHwSpec">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/hwSpecform"/>
                <verifytitle description="we should see the hwSpecDetail title"
                    text=".*${hwSpecDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <setInputField description="set name" name="name" value="CoFfKwWcXuBwTtMdTyLlDaFtMkYeEn"/>

                <clickbutton label="${button.save}" description="Click button 'Save'"/>
                <verifytitle description="HwSpec List appears if save successful"
                    text=".*${hwSpecList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${hwSpec.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing hwSpec -->
    <target name="DeleteHwSpec" description="Deletes existing HwSpec">
        <webtest name="deleteHwSpec">
            &config;
            <steps>
                &login;
                <invoke description="click Edit HwSpec link" url="/hwSpecform?id=-1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display HwSpec List" text=".*${hwSpecList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${hwSpec.deleted}"/>
            </steps>
        </webtest>
    </target>    <!--HwSpec-END-->
    <!--HwCert-START-->
    <!-- runs hwCert-related tests -->
    <target name="HwCertTests"
            depends="SearchHwCerts,EditHwCert,SaveHwCert,AddHwCert,DeleteHwCert"
            description="Call and executes all hwCert test cases (targets)">
        <echo>Successfully ran all HwCert UI tests!</echo>
    </target>

    <!-- Verify the hwCerts list screen displays without errors -->
    <target name="SearchHwCerts" description="Tests search for and displaying all hwCerts">
        <webtest name="searchHwCerts">
            &config;
            <steps>
                &login;
                <invoke description="click View HwCert link" url="/hwCerts"/>
                <verifytitle description="we should see the hwCertList title"
                    text=".*${hwCertList.title}.*" regex="true"/>
                <setinputfield description="set search term" name="q" value="*"/>
                <clickbutton label="${button.search}" description="Click Search"/>
                <verifytitle description="we should see the hwCertList title"
                    text=".*${hwCertList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the edit hwCert screen displays without errors -->
    <target name="EditHwCert" description="Tests editing an existing HwCert's information">
        <webtest name="editHwCert">
            &config;
            <steps>
                &login;
                <invoke description="View HwCert List" url="/hwCerts"/>
                <clicklink label="-1" description="Click edit link"/>
                <verifytitle description="we should see the hwCertDetail title"
                    text=".*${hwCertDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a hwCert and then save -->
    <target name="SaveHwCert" description="Tests editing and saving a hwCert">
        <webtest name="saveHwCert">
            &config;
            <steps>
                &login;
                <invoke description="click Edit HwCert link" url="/hwCertform?id=-1"/>
                <verifytitle description="we should see the hwCertDetail title"
                    text=".*${hwCertDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <setInputField description="set name" name="name" value="VtNkEyXlRpKePrVmKrDgQqFfJxDzGk"/>

                <clickbutton label="${button.save}" description="Click Save"/>
                <verifytitle description="Page re-appears if save successful"
                    text=".*${hwCertDetail.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${hwCert.updated}"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new HwCert -->
    <target name="AddHwCert" description="Adds a new HwCert">
        <webtest name="addHwCert">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/hwCertform"/>
                <verifytitle description="we should see the hwCertDetail title"
                    text=".*${hwCertDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <setInputField description="set name" name="name" value="FxTrBdAvFnViBpMoPpKnFxLpTfIxQv"/>

                <clickbutton label="${button.save}" description="Click button 'Save'"/>
                <verifytitle description="HwCert List appears if save successful"
                    text=".*${hwCertList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${hwCert.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing hwCert -->
    <target name="DeleteHwCert" description="Deletes existing HwCert">
        <webtest name="deleteHwCert">
            &config;
            <steps>
                &login;
                <invoke description="click Edit HwCert link" url="/hwCertform?id=-1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display HwCert List" text=".*${hwCertList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${hwCert.deleted}"/>
            </steps>
        </webtest>
    </target>    <!--HwCert-END-->
</project>

























